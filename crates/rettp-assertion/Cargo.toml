[package]
name = "rettp-assertion"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.10"
indexmap = "2.14"
serde_json = "1.0"
thiserror = "2.0"
rettp-domain = { path = "../rettp-domain" }
rettp-http = { path = "../rettp-http" }
